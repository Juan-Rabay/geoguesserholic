/* English translation layer. Missing fields fall back to the Portuguese base.
   Country names come from each entry's `en` field, so no name map is needed. */
window.GG = window.GG || {};
GG.tr = GG.tr || {};
GG.tr.en = GG.tr.en || {};
